interface:
  display_name: "Loop Optimized Planner"
  short_description: "Build lean loops that stay aligned over time"
  default_prompt: "Use $loop-optimized-planner to create the smallest durable, goal-aligned loop for this work."
policy:
  allow_implicit_invocation: true
